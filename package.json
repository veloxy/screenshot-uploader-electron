{
  "name": "ScreenshotUploaderElectron",
  "version": "1.0.0",
  "description": "Screenshot uploader created in electron.",
  "main": "src/script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/ && electron-packager ./ ScreenshotUploader --platform=darwin --arch=all --version=0.36.7 --out=./build --ignore=build/ --overwrite --prune"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.5.0",
    "angular-ui-router": "^0.2.18",
    "aws-sdk": "^2.2.37",
    "chokidar": "^1.4.2",
    "copy-paste": "^1.1.4",
    "electron-settings": "^1.0.2",
    "google-url": "0.0.4",
    "methodical": "0.0.3",
    "mime": "^1.3.4"
  },
  "devDependencies": {
    "electron-packager": "^5.2.1"
  }
}
